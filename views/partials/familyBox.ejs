<div class="instrument-box">
  <a href="/family/<%= family.id %>" class="family-link">
    <h3><%= family.family %></h3>
  </a>
    <form action="/family/<%= family.id %>/edit" method="GET">
        <button type="submit" class="edit">Edit</button>
    </form>
    <form action="/family/<%= family.id %>/delete" method="GET">
        <button type="submit" class="delete">Delete</button>
    </form>
</div>